<%= semantic_form_for [@registration, @notification_address] do |f| %>
	<%= f.inputs :first_name, :last_name, :email %>
	<%= f.input :registration_id, :as => :hidden %>
	<%= f.buttons do %>
		<%= f.commit_button %>
	<% end %>
<% end %>