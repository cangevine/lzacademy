<% content_for :head do %>
<%= formtastic_stylesheet_link_tag %>
<% end %>

<h1>Write a new comment</h1>
<p>for <%= @registration.student.display_name %> in <%= @registration.course.program.name %></p>
<%= semantic_form_for [@registration, @comment] do |f| %>
	<%= render :partial => "form", :locals => { :f => f, :reg => @registration, :admin => false } %>
<% end %>